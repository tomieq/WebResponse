## Overview
SPM library for sending web requests. It works on MacOS and Linux.

## Testing

Test on macOS: `swift test`
Test on linux: `docker run --rm -t  -v "$PWD":/workspace -w /workspace swift:6.1 swift test`
